name: Deploy to Lolipop

on:
  workflow_dispatch: # 手動実行
  push:
    branches:
      - master # masterブランチへのpushで自動実行

jobs:
  build-and-deploy:
    name: ビルド & デプロイ
    runs-on: ubuntu-latest

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 依存関係のインストール
        run: npm ci

      - name: ビルド実行
        run: npm run build

      - name: ビルド成果物の確認
        run: |
          echo "📦 ビルド完了。以下のファイルをデプロイします："
          ls -la out/

      - name: FTPアップロードスクリプトを作成
        run: |
          cat > upload.sh << 'SCRIPT'
          #!/bin/bash
          set -e

          # FTP_REMOTE_DIRが設定されていない場合はエラー
          if [ -z "$FTP_REMOTE_DIR" ]; then
            echo "❌ エラー: FTP_REMOTE_DIR が設定されていません"
            echo "GitHub Secrets に FTP_REMOTE_DIR を追加してください（例: public_html または public_html/ など）"
            exit 1
          fi

          # 末尾のスラッシュを削除（存在する場合）
          REMOTE_DIR="${FTP_REMOTE_DIR%/}"

          echo "📤 ファイルをアップロード中..."
          echo "📁 アップロード先: $REMOTE_DIR/"

          # outディレクトリ内の全ファイルを再帰的にアップロード
          cd out
          find . -type f -not -path '*/\.*' | while read file; do
            # 先頭の ./ を削除
            clean_file="${file#./}"

            # アップロード先のフルパス
            remote_path="$REMOTE_DIR/$clean_file"

            # ファイルをアップロード
            echo "Uploading: $clean_file → $remote_path"

            # curlでアップロード
            curl -u "$FTP_USERNAME:$FTP_PASSWORD" \
              --ftp-create-dirs \
              --ftp-pasv \
              --upload-file "$file" \
              "ftp://$FTP_SERVER/$remote_path" \
              --max-time 300 \
              --retry 3 \
              --retry-delay 5 \
              --silent \
              --show-error

            if [ $? -eq 0 ]; then
              echo "  ✓ Success"
            else
              echo "  ✗ Failed"
              exit 1
            fi
          done

          echo "✅ アップロード完了"
          SCRIPT

          chmod +x upload.sh

      - name: ロリポップへデプロイ
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_REMOTE_DIR: ${{ secrets.FTP_REMOTE_DIR }}
        run: ./upload.sh

      - name: デプロイ完了
        run: echo "✅ ロリポップへのデプロイが完了しました"
