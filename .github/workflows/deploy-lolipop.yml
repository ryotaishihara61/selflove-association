name: Deploy to Lolipop

on:
  workflow_dispatch: # 手動実行
  push:
    branches:
      - master # masterブランチへのpushで自動実行

jobs:
  build-and-deploy:
    name: ビルド & デプロイ
    runs-on: ubuntu-latest

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 依存関係のインストール
        run: npm ci

      - name: ビルド実行
        run: npm run build

      - name: ビルド成果物の確認
        run: |
          echo "📦 ビルド完了。以下のファイルをデプロイします："
          ls -la out/

      - name: lftpのインストール
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: ロリポップへデプロイ
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -c "
          set ftp:ssl-allow no;
          set net:timeout 10;
          set net:max-retries 3;
          open -u $FTP_USERNAME,$FTP_PASSWORD $FTP_SERVER;
          mirror --reverse --delete --verbose --exclude .git/ --exclude .github/ ./out/ /;
          bye
          "

      - name: デプロイ完了
        run: echo "✅ ロリポップへのデプロイが完了しました"
